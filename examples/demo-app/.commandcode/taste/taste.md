# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# Workflow
- Work within the repo directory using relative paths instead of absolute paths for file operations. Confidence: 0.70
- Prefer simpler workflows with fewer steps; prioritize DX improvements when the user finds a multi-step flow too complex. Confidence: 0.75

# Data Integrity
- Never allow redacted values to be vaulted; the vault must always contain the original real values to prevent permanent data loss. Confidence: 0.80

